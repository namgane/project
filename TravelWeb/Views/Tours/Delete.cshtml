@model TravelWeb.Models.Tour

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Tour</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.TenTour)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.TenTour)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.DiaDiem)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.DiaDiem)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.MoTa)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.MoTa)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.SoNgay)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.SoNgay)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Gia)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Gia)</dd>

        <dt class="col-sm-2">Hình Ảnh</dt>
        <dd class="col-sm-10">
            @if (!string.IsNullOrEmpty(Model.HinhAnh))
            {
                <img src="@Model.HinhAnh" alt="@Model.TenTour" style="max-width:300px; max-height:300px;" class="img-thumbnail" />
            }
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
